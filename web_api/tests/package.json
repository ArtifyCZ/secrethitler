{
	"scripts": {
		"test": "cargo build --release && clear && jest -i",
        "coverage": "jest --collectCoverage -i"
	},
	"dependencies": {
		"jest": "^27.5.1",
		"supertest": "^6.2.2",
		"unique-username-generator": "^1.0.1"
	}
}
